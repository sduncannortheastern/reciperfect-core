module.exports = {
  testEnvironment: 'node',
  clearMocks: true,
  // Optional: setup file to run before each test suite (e.g., for env vars)
  // setupFilesAfterEnv: ['./jest.setup.js'],
  // Optional: coverage reporting
  // collectCoverage: true,
  // coverageDirectory: "coverage",
  // coverageProvider: "v8",
};
